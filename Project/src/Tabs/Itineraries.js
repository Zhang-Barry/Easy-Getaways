import * as React from 'react';
import { View, Text } from 'react-native';
import ItineraryRectangularList from '../ItineraryRectangularList';

export default function Itineraries({ navigation }) {
    return (
        <ItineraryRectangularList/>
    );
}