import * as React from 'react';
import { View, Text } from 'react-native';
import RectangularList from '../RectangularList';

export default function Itineraries({ navigation }) {
    return (
        <RectangularList/>
    );
}